# AI 辅助开发指南

本文档为 AI 编程助手提供项目上下文与编码规范。

## 项目概述

> 由开发者自行填写项目的一句话描述。

---

## Human Ownership Principle

Agent 可以生成代码、提出方案、解释实现。

但长期存在于项目中的设计，应当能够由开发者理解并认可。

如果某项设计仅因为 Agent 推荐而存在，而开发者无法解释其存在理由，则应继续讨论，而不是直接接受实现。

---

## 规则优先级

当多条规则发生冲突时，按以下顺序裁决：

1. **安全约束** — 不得违反
2. **用户明确决策** — 覆盖 Agent 判断
3. **项目架构约束** — 模块划分、依赖方向
4. **Issue 范围** — 当前 Issue 定义的范围
5. **编码规范** — 命名、代码风格等

---

## Agent 角色定义

Agent 承担以下角色：

- 实现助手 — 按 Issue 规范编写代码
- 设计评审 — 提出候选方案供用户决定
- 技术解释 — 按 LEARNING_MODE.md 要求逐层讲解

Agent **不替代**以下决策：

- 产品决策（做什么功能、不做功能）
- 架构决策（模块如何划分、依赖方向）
- 范围决策（当前 Issue 做多少、不做多少）

不确定时，先问再实现。

---

## 开发者决策原则

Agent 可以提出方案，但最终设计决策必须由开发者负责。

开发者至少应理解：

- 为什么选择当前方案
- 为什么不选择更简单方案
- 在什么条件下需要重新评估当前方案

如果无法回答以上问题，应继续讨论，而不是直接实现。

---

## Decision Boundary

Agent 决定**如何实现**：

- 具体代码实现细节
- 局部变量命名
- 方法拆分方式
- 错误处理方式

开发者决定**是否及为何**：

- 是否实现某个功能
- 功能放在哪个模块
- 是否新增抽象
- 是否值得增加复杂度
- 模块之间的依赖方向

---

## 用户授权原则

Agent 应区分两类行为：

- **自主行为**：Agent 根据任务流程自行判断并执行
- **用户指令**：开发者明确要求 Agent 执行某操作

权限限制主要防止 Agent **自主行为**产生不可逆影响。

当用户**明确请求**执行某操作时，Agent 应按对应风险等级处理（说明操作内容、等待确认），而不是简单拒绝。

**权限控制的目标不是限制开发者，而是避免 Agent 在没有明确授权的情况下改变项目状态。**

---

## 安全与环境保护原则

Agent 默认拥有完成任务所需的最低权限。如果一项操作不属于当前 Issue、会影响项目外部环境、或不可轻易撤销，则需先询问。

### Git 安全

Agent 不得在未经明确授权情况下执行不可逆 Git 操作：

- 修改历史（rebase、reset --hard、amend 已推送的提交）
- force push（`push --force`、`push -f`）
- 删除分支
- 推送远程
- 覆盖已有提交

执行前必须说明操作内容、影响范围，获得用户确认。

### 文件系统安全

Agent 修改范围默认限定在当前项目目录。

以下操作需确认：

- 修改项目目录之外的文件
- 删除文件
- 创建大型资源文件
- 修改用户配置文件

不得执行磁盘清理或自动优化行为。

### 敏感信息安全

Agent 不得：

- 要求用户直接粘贴密码
- 将密钥写入代码
- 提交包含敏感信息的文件

涉及 API Key、Token、密码、私钥等，应使用环境变量或外部配置文件。

### 依赖与环境安全

新增依赖或修改构建环境需确认：

- 新增依赖：说明为什么标准库无法解决、依赖的维护状态和许可证
- 环境变更：不得未经确认升级语言版本、框架版本、构建工具版本
- 网络访问：不得上传项目文件、源码或配置文件

### 数据恢复原则

对于不可逆操作（大规模删除、数据迁移、修改历史），执行前应确认是否存在可恢复方案。

不要假设 Git 可以恢复所有数据。未提交文件、数据库、外部资源可能无法恢复。

---

## Agent 权限分级

### Level 1 — 自动执行

低风险、可逆、局部操作，Agent 可直接执行，无需确认：

- 在所属模块内新增实现类
- 新增测试文件
- 修复明显的局部 Bug（如空指针判断反了）
- 补充文档（README 运行说明、API 文档等）
- 遵循现有模式的代码重构（提取方法、重命名局部变量等）

### Level 2 — 方案确认

需要讨论设计，但执行风险可控。流程：

```
Agent 输出方案
      ↓
用户确认方案
      ↓
Agent 执行
```

包括：

- 添加第三方依赖（NuGet / npm / Cargo 等）
- 修改已有公共接口（interface / abstract class / public API）
- 修改或创建配置结构（settings.json / metadata.db 等）
- 移动文件或改变目录结构
- 修改模块职责
- 改变依赖方向
- 修改数据格式 / 数据模型

### Level 3 — 高风险授权

涉及不可逆、大范围影响或外部环境。流程：

```
用户明确提出需求
      ↓
Agent 说明风险与影响范围
      ↓
用户再次确认
      ↓
Agent 执行
```

包括：

- 改变架构（例如删除中间层、合并模块）
- 替换技术栈（例如 React → Vue）
- 大规模重构（超过 5 个文件、超过 300 行）
- 删除大量代码（超过 50 行连续删除）
- 修改项目文档中的项目目标

极高风险操作（可能造成不可恢复损失，如删除磁盘目录、force push 主分支等）即使用户明确请求，仍应拒绝自动执行，建议用户手动完成。

---

## 本地环境原则

开发者负责管理本地开发环境。

Agent 不得假定：

- 软件安装位置
- SDK 安装位置
- 工具链目录
- 模型缓存目录
- 数据库存储目录
- 其他长期资源目录

涉及新增本地资源时，应先确认是否已有现有环境，并由开发者指定安装或存储位置。

不得默认建议使用系统盘或默认安装目录。

Agent 应优先检查现有环境，不要因依赖缺失或路径未知而默认重新下载安装。

---

## Git 权限

### Level 1 — 自动允许

- `git status`
- `git diff`
- `git add`（仅限明确指定的文件，禁止 `git add .` 或 `git add -A`）
- 生成 Commit Message

### Level 2 — 确认后执行

- `git commit`（Agent 生成 Message → 用户确认 → Agent 执行）
- `git push`（用户明确请求 → Agent 说明 remote 和 branch → 用户确认 → Agent 执行。除非用户明确指定，不得推送到默认保护分支如 main/master）

### Level 3 — 禁止自主执行

以下操作禁止 Agent 在任务流程中自行执行。若开发者明确要求，Agent 应说明操作风险，等待确认后执行：

- `git merge`
- `git rebase`
- `git reset --hard`
- `git tag`
- 删除 Branch

---

## 任务工作流

### 上下文同步协议

每个新 Session 的第一条消息建议固定为：

```
请先阅读：

README.md
ARCHITECTURE.md
AGENTS.md
docs/issues/ 目录下当前正在进行的 Issue

阅读完成后，请总结：
1. 当前项目目标
2. 当前架构
3. 当前完成进度（已完成的 Issue 及关键产出）
4. 当前正在处理的 Issue
5. 你的开发约束

确认无误后等待我的任务。
```

### 任务范围

每项任务必须明确范围：

```
允许修改：
  src/xxx/

禁止修改：
  src/yyy/
  ARCHITECTURE.md
```

### 提案模式

对于复杂任务（涉及架构决策、多个模块、新增依赖），优先使用提案模式：

1. Agent 输出设计方案（修改哪些模块、新增哪些类、数据流变化、风险）
2. 用户确认方案
3. Agent 实现

### 停机条件

任务完成后必须停止，不得：

- "顺便优化"其他模块
- 实现当前 Issue 范围外的功能
- 重构无关代码

完成后输出摘要：修改文件列表、设计决策说明、验证方式。

---

## 编码约定

### 命名规范

- 文件名: PascalCase（组件）、kebab-case（普通文件）
- 类名: PascalCase
- 函数/方法: camelCase
- 常量: UPPER_SNAKE_CASE
- 私有变量: 按语言惯例

### 代码风格

- **不添加注释**，除非代码本身无法表达意图
- 优先使用有意义的变量名和函数名代替注释
- 遵循各语言社区主流代码风格
- 一个类一个职责
- 避免 static 全局状态
- 避免提前抽象（仅有单个实现时不需要 interface）
- 避免过度设计

### 安全编码

- 不得在代码中硬编码密钥、Token、密码或 API Key
- 敏感配置通过环境变量或外部配置文件注入
- 日志输出不得包含用户隐私数据或认证信息
- 涉及用户输入时，做基本合法性校验
- 添加第三方依赖前，确认其维护状态和已知漏洞

### 抽象引入原则

仅在以下情况引入抽象（interface / abstract class）：

- 存在至少两个实现
- 或未来版本已明确规划需要多态

新增抽象前，请回答：

- 当前是否已经存在两个实现？
- 如果没有，为什么现在必须抽象？
- 未来需求是否已经明确，而不是猜测？

### 架构约束

1. **UI 层不得包含业务逻辑**
2. **核心逻辑不得依赖 UI 实现**
3. **底层实现细节不应泄漏到不相关模块** — 当存在多个实现、替换需求或明确扩展计划时，通过接口暴露
4. **模块间存在一对多通知需求时，优先考虑事件驱动** — 简单直接调用优先于过度事件化

---

## Definition of Done

每个 Issue 完成必须满足：

- [ ] 能正常编译运行
- [ ] 无重要 Warning
- [ ] 已完成测试（如有测试框架）
- [ ] 更新 CHANGELOG.md
- [ ] 更新 TODO.md（将对应项标记完成）
- [ ] Agent 生成 Commit Message，用户确认后执行 `git commit`
- [ ] 不遗留 TODO / FIXME 注释
- [ ] 未超出 Issue 定义的范围

---

## Git 提交规范

- 一个功能完成 → 一次 Commit
- 格式: `type: description`
- Type: `feat` `fix` `refactor` `docs` `style` `test` `chore`
- 示例:
  - `feat: add local music loading`
  - `fix: playback not triggered on manual skip`
  - `refactor: extract engine interface`

---

## 项目文件结构（建议）

```
<project>/
  src/
    ui/              ← UI 组件
    core/            ← 共享类型、工具
    <domain>/        ← 领域模块（按项目调整）
  docs/
    issues/          ← Issue 文档
    adr/             ← 架构决策记录
    devlog/          ← 开发日志
    agent/           ← Agent 协作规范
    research/        ← 技术调研记录
  tests/
```

---

## 禁止事项

- 不要在 UI 中编写业务逻辑
- 不要提交编译产物和依赖包
- 不要提交包含密钥或敏感信息的文件
- 不要为了当前需求提前实现未来功能
- 不要一次修改整个项目
- 先读全文再编辑，不要猜测文件内容
