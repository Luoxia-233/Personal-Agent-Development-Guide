# 架构设计

> 模板。将 `<placeholder>` 替换为实际内容，删除本行。

## 分层架构

```
（用 ASCII 图描述各层及其职责）
```

## 模块职责

### <模块名>

- 职责1
- 职责2
- **不包含**：职责边界外的内容

## 依赖方向

```
（描述模块间的允许和禁止的依赖方向）
```

## 核心原则

1. **面向接口编程** — 模块之间通过抽象接口通信
2. **事件驱动** — 跨模块通信优先使用事件
3. **可替换性** — 底层实现可通过接口替换
4. **渐进增强** — MVP 先验证流程，后续版本逐步加入高级特性
